# ============================================================
# DocsChat 환경변수 설정 파일
# 이 파일을 복사하여 .env로 저장하고 값을 채워주세요:
#   cp .env.example .env
# ============================================================

# ===== LLM 설정 =====
# 제공자: openai | anthropic | google | ollama
LLM_PROVIDER=openai

# 모델명 (비워두면 제공자별 기본값 사용)
# OpenAI: gpt-4o-mini, gpt-4o, gpt-3.5-turbo
# Anthropic: claude-3-5-sonnet-20241022, claude-3-haiku-20240307
# Google: gemini-1.5-flash, gemini-1.5-pro
# Ollama: llama3.2, llama3.1, mistral, qwen2.5
LLM_MODEL=gpt-4o-mini

# API Keys (사용하는 제공자의 키만 설정하세요)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=AIza...

# ===== 임베딩 설정 =====
# 제공자: huggingface (무료/로컬) | openai (유료)
EMBEDDING_PROVIDER=huggingface

# 임베딩 모델명 (비워두면 제공자별 기본값 사용)
# HuggingFace: sentence-transformers/all-MiniLM-L6-v2, BAAI/bge-m3
# OpenAI: text-embedding-3-small, text-embedding-3-large
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# ===== ChromaDB 설정 =====
# Docker 내부에서는 서비스명으로 연결
CHROMA_HOST=chromadb
CHROMA_PORT=8000
CHROMA_COLLECTION=docschat

# ===== Ollama 설정 (Ollama 사용 시) =====
# Docker 내부에서는 서비스명으로 연결
OLLAMA_HOST=ollama
OLLAMA_PORT=11434
